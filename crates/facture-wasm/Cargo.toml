[package]
name = "facture-wasm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Wrapper wasm-bindgen autour de facture-core : validate(invoiceJson) -> reportJson, exécuté dans le task pane."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
facture-core = { path = "../facture-core" }
wasm-bindgen = "0.2"
